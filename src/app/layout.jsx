import { Providers } from '@/app/providers'
import { Layout } from '@/components/Layout'

import '@/styles/tailwind.css'

export const metadata = {
  title: {
    template: '%s - Benesh Khan',
    default:
      'Benesh Khan - Clinical Psychologist, Educator, and Mental Health Advocate',
  },
  description:
    "I'm Benesh Khan, a Clinical Psychology graduate with a Master's degree from SZABIST, Islamabad. I specialize in psychological assessment, evidence-based therapy (CBT, ERP), and child development.",
  alternates: {
    types: {
      'application/rss+xml': `${process.env.NEXT_PUBLIC_SITE_URL}/feed.xml`,
    },
  },
}

export default function RootLayout({ children }) {
  return (
    <html lang="en" className="h-full antialiased" suppressHydrationWarning>
      <body className="flex h-full bg-zinc-50 dark:bg-black">
        <Providers>
          <div className="flex w-full">
            <Layout>{children}</Layout>
          </div>
        </Providers>
      </body>
    </html>
  )
}
